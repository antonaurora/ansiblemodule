---
collection_info:
  name: crmit
  version: 1.0.0
  authors:
    - Ваше имя <anton@td-aurora.ru>
  description: Коллекция получения данных из собственной срм 
  license:
    - GPL-3.0-or-later
  tags:
    - custom
    - modules
    - crmit
  dependencies: {}

# Зависимости от других коллекций (если есть)
dependencies:
  # - name: community.general
  #   version: ">=5.0.0"
  # - name: ansible.posix
  #   version: ">=1.5.0"

# Пути к плагинам (обычно не нужно, Ansible сам найдет)
# Требования к версии Ansible
requires_ansible: '>=2.9.0'
